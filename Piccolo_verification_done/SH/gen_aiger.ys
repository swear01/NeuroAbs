read_verilog -formal iter_sanity/test.v
#prep -flatten -top wrapper
hierarchy -check -top wrapper
proc
#flatten
#prep -top wrapper

chformal -assume -early;
select wrapper/sanitycheck
chformal -remove
select *

memory ;
flatten
setundef -undriven -expose;
sim -clock clk -reset rst -rstlen 1 -n 1 -w wrapper
delete -output
techmap
#aigmap
abc -fast -g AND
#opt
write_aiger -zinit -vmap map.txt test.aig
