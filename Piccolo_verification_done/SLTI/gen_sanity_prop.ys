read_verilog -formal wrapper.v
hierarchy -check -top wrapper
proc
chformal -assume -early;

select wrapper/normalassert
chformal -remove
select *

memory ;
flatten
setundef -undriven -expose;
sim -clock clk -reset rst -rstlen 1 -n 1 -w wrapper
write_btor  -s sanity.btor2
